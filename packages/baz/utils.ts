/**
 * Capitalizes the input string.
 *
 * @param input - The string to be capitalized.
 * @returns The capitalized string.
 */
export function capatalize(input: string): string {
  return input.toUpperCase();
}
